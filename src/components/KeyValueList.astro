---
interface Item {
  label: string;
  value: string;
}

interface Props {
  items: Item[];
}

const { items } = Astro.props;
---
<dl class="key-value-list">
  {items.map((item) => (
    <>
      <dt>{item.label}</dt>
      <dd>{item.value}</dd>
    </>
  ))}
</dl>
