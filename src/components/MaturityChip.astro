---
type Maturity = "stub" | "reviewed" | "full";

interface Props {
  maturity: Maturity;
  label?: string;
}

const { maturity, label = maturity.replaceAll("_", " ") } = Astro.props;
---
<span class={`chip chip-${maturity}`}>{label}</span>
