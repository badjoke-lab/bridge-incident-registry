name: Review Archive Capture Batch 2

on:
  push:
    branches:
      - agent/phase3-archive-capture-batch2-review
    paths:
      - "scripts/review-archive-capture-batch2.mjs"
      - ".github/workflows/review-archive-capture-batch2.yml"
  pull_request:
    paths:
      - "scripts/review-archive-capture-batch2.mjs"
      - ".github/workflows/review-archive-capture-batch2.yml"
  workflow_dispatch:

permissions:
  contents: write

jobs:
  review:
    runs-on: ubuntu-latest
    steps:
      - name: Checkout branch
        uses: actions/checkout@v4
        with:
          ref: agent/phase3-archive-capture-batch2-review
          fetch-depth: 0

      - name: Setup Node
        uses: actions/setup-node@v4
        with:
          node-version: 22

      - name: Verify Wayback candidates
        run: node scripts/review-archive-capture-batch2.mjs

      - name: Commit verification report
        run: |
          git config user.name "github-actions[bot]"
          git config user.email "41898282+github-actions[bot]@users.noreply.github.com"
          git add docs/audits/phase3-archive-capture-batch2-candidate-verification.json
          git diff --cached --quiet && exit 0
          git commit -m "docs: capture archive Batch 2 candidate verification [inventory]"
          git pull --rebase origin agent/phase3-archive-capture-batch2-review
          git push origin HEAD:agent/phase3-archive-capture-batch2-review
