name: Apply Event Primary Remediation 02 v2

on:
  workflow_dispatch:

permissions:
  contents: write

jobs:
  apply:
    runs-on: ubuntu-latest
    steps:
      - run: echo "Temporarily disabled while the bounded applicator is updated."
