name: Verify Verus May Native and Series Production

on:
  pull_request:
    branches: [main]

permissions:
  contents: read

jobs:
  verify-production:
    if: github.head_ref == 'agent/verus-may-production-verification'
    runs-on: ubuntu-22.04
    timeout-minutes: 25
    steps:
      - name: Checkout exact current main
        uses: actions/checkout@v7
        with:
          ref: main
          fetch-depth: 0

      - name: Pin exact merged main
        run: test "$(git rev-parse HEAD)" = "b72aa190f07a11f45baa2cfcf57ae9295343b374"

      - name: Setup Node
        uses: actions/setup-node@v7
        with:
          node-version: 22

      - name: Install dependencies
        run: npm install --no-audit --no-fund

      - name: Build and validate exact-main native plus Series output
        run: |
          set -euo pipefail
          npm run build
          node scripts/check-series-adapter.mjs
          node scripts/check-dist-consistency.mjs
          jq -e '.record_counts.primary_records == 39 and .record_counts.series_records == 81 and .routes.search == "/incidents/" and .routes.compare == "/compare/" and .routes.stats == "/stats/"' public/data/series/registry.json >/dev/null
          jq -e '.record_count == 81 and .record_counts.bridges == 39 and .record_counts.incidents == 42' public/data/series/index.json >/dev/null

      - name: Verify native production equality
        env:
          PUBLIC_SITE_ORIGIN: https://bir.badjoke-lab.com
          BIR_PRODUCTION_TIMEOUT_MS: "30000"
        run: npm run production:verify

      - name: Verify complete Series adapter on production
        env:
          SERIES_ORIGIN: https://bir.badjoke-lab.com
        run: |
          set -euo pipefail
          node --input-type=module <<'NODE'
          import fs from 'node:fs';
          import path from 'node:path';

          const root = path.resolve('public/data/series');
          const origin = process.env.SERIES_ORIGIN;
          const sleep = (ms) => new Promise((resolve) => setTimeout(resolve, ms));

          function listJson(dir, base = dir) {
            const out = [];
            for (const entry of fs.readdirSync(dir, { withFileTypes: true })) {
              const target = path.join(dir, entry.name);
              if (entry.isDirectory()) out.push(...listJson(target, base));
              else if (entry.isFile() && entry.name.endsWith('.json')) out.push(path.relative(base, target).split(path.sep).join('/'));
            }
            return out.sort();
          }

          function normalize(value) {
            if (Array.isArray(value)) return value.map(normalize);
            if (value && typeof value === 'object') {
              return Object.fromEntries(Object.keys(value).sort().filter((key) => key !== 'generated_at').map((key) => [key, normalize(value[key])]));
            }
            return value;
          }

          const files = listJson(root);
          if (files.length !== 83) throw new Error(`expected 83 Series JSON files, found ${files.length}`);
          const local = new Map(files.map((file) => [file, normalize(JSON.parse(fs.readFileSync(path.join(root, file), 'utf8')))]));

          let lastError;
          for (let attempt = 1; attempt <= 60; attempt++) {
            try {
              for (const file of files) {
                const url = `${origin}/data/series/${file}?verify=b72aa190-${attempt}-${Date.now()}`;
                const response = await fetch(url, { headers: { accept: 'application/json' } });
                if (!response.ok) throw new Error(`${file}: HTTP ${response.status}`);
                const remote = normalize(await response.json());
                if (JSON.stringify(remote) !== JSON.stringify(local.get(file))) throw new Error(`${file}: semantic mismatch`);
              }
              const indexResponse = await fetch(`${origin}/data/series/index.json?verify=keys-${Date.now()}`);
              if (!indexResponse.ok) throw new Error(`index.json: HTTP ${indexResponse.status}`);
              const index = await indexResponse.json();
              const keys = index.records.map((record) => record.global_record_key);
              if (keys.length !== 81 || new Set(keys).size !== 81) throw new Error('global-key uniqueness/count mismatch');
              console.log(`BIR Series production equality PASS attempt=${attempt} files=${files.length} records=${keys.length}`);
              lastError = null;
              break;
            } catch (error) {
              lastError = error;
              console.log(`Attempt ${attempt}/60 failed: ${error.message}`);
              await sleep(10000);
            }
          }
          if (lastError) throw lastError;
          NODE

      - name: Verify Verus May July and representative human routes
        run: |
          set -euo pipefail
          for url in \
            https://bir.badjoke-lab.com/bridge/verus-ethereum-bridge/ \
            https://bir.badjoke-lab.com/incident/verus-ethereum-bridge-2026-may-import-verification-exploit/ \
            https://bir.badjoke-lab.com/incident/verus-ethereum-bridge-2026-july-import-verification-exploit/ \
            https://bir.badjoke-lab.com/incidents/ \
            https://bir.badjoke-lab.com/compare/ \
            https://bir.badjoke-lab.com/stats/; do
            code="$(curl -sS -o /dev/null -w '%{http_code}' "${url}?verify=b72aa190-$(date +%s)")"
            test "$code" -ge 200 && test "$code" -lt 400
            echo "$url -> $code"
          done
